server.port=8081
spring.application.name=user-service
spring.datasource.url=jdbc:postgresql://localhost:5432/flight-booking
spring.datasource.username=${USERNAME:db_user}
spring.datasource.password=${PASSWORD:123qwe}
spring.threads.virtual.enabled=true

server.http2.enabled=true

opentracing.jaeger.http-sender.url=http://localhost:14268/api/traces
opentracing.jaeger.log-spans=true

management.endpoints.web.exposure.include=health, info, prometheus, metrics